---
# SCRAM credentials for fraud-analytics (worked example).
# Placeholder password so the demo runs; replace with a SealedSecret / External
# Secret before any real use. The client-side value must match.
apiVersion: v1
kind: Secret
metadata:
  name: fraud-analytics-kafka-scram
  namespace: kong
  labels:
    app.kubernetes.io/managed-by: backstage
    business-unit: wealth-management-la
type: Opaque
stringData:
  username: "fraud-analytics"
  password: "REPLACE_ME"
