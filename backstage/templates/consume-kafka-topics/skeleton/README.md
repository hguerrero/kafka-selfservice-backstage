# ${{ values.appName }} — Kafka access

Self-service Kafka access for **${{ values.appName }}**, generated by Backstage.

- **Provider:** ${{ values.businessUnit }}
- **Auth:** ${{ values.authMode }}
- **Broker prefix (hidden from client):** `${{ values.namespacePrefix }}`
- **Bootstrap:** `bootstrap.${{ values.appName }}.127-0-0-1.sslip.io:9092`

## Topics granted

{% for topic in values.topics %}- `${{ topic }}`
{% endfor %}

## What's in here

| File | Purpose |
|------|---------|
| `kong/virtual-cluster.yaml` | Dedicated virtual cluster + authentication + prefix hiding |
| `kong/acl-policy.yaml` | Read ACLs for the topics above — **edit this to change access** |
| `kong/tlsroute.yaml` | SNI route so clients reach this virtual cluster |
| `kong/scram-credentials.yaml` | SCRAM secret (SCRAM auth only) |
| `catalog-info.yaml` | Backstage Component for this app |

On merge, Argo CD applies `kong/` and Kong Operator reconciles the changes into the
Konnect control plane and the Event Gateway data plane.

## Add more topics later

Run the **Add Topics to Application** template in Backstage (or edit
`kong/acl-policy.yaml` directly). Only the ACL policy changes — the virtual cluster,
credentials and route stay exactly as they are.
